<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.RunRevolution" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:fontFamily">@font/montserrat_regular</item>
        <item name="colorPrimary">#226CE0</item>

        <!-- Defining that new color in ColorPrimaryDark -->
        <item name="android:statusBarColor">#1D348A</item>
        <item name="android:windowTranslucentStatus">true</item>
    </style>

    <style name="Theme.RunRevolution" parent="Base.Theme.RunRevolution" />
</resources>